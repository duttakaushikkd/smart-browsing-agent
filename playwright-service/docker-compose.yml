version: "3.8"

services:
  playwright-service:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ENVIRONMENT=production
      - LOG_LEVEL=INFO
      - PORT=8080
    restart: always
